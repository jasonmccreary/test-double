Double `BookRepository` received a call to `find(456)` that doesn't match any of its configured expectations.

The following similar call was made to `find`:
  id:
    - 123
    + 456

Note: this double is in passthru mode, but using `expects()` means every call needs to be configured. Use `allows()` instead if you want unmatched calls to run for real.